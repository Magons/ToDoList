$(function() {
	$('#content').on("click", '.task_up', function(){
		debugger
		var task_id = $(this).data('task-id');
		var project_id =  $(this).data('project-id');
		$.ajax({
      type: "patch",
      url: "/projects/"+project_id+"/tasks/task_up/" + task_id,
      data: { },
      success: function(){
      	
	      },
      error: function(e){
      	error_title = JSON.parse(e.responseText)['title'][0];
      	$('.flash').append(showFlashMesage('<p class="alert alert-danger">Title '+ error_title +'</p>'));
      }
  	});
	});

	$('#content').on("click", '.task_down', function(){
		var task_id = $(this).data('task-id');
		var project_id =  $(this).data('project-id');
		$.ajax({
      type: "patch",
      url: "/projects/"+project_id+"/tasks/task_down/" + task_id,
      
  	});
	});
});