div.projects
  - @projects.each do |project|
    = render 'project', project: project       
div.center
  = button_tag class: "ngdialog-button ngdialog-button-secondary", data: { toggle: 'modal', target: '#myModal' } do
    = image_tag ("plus_for_btn.fw.png")
    | Add TODO List


/ ul
/   - @projects.each do |project|
/     div id="tasks_errors_#{project.id}"
/     li
/       = project.title
/       span
/         = link_to ' Show ', project
/       span
/         = link_to ' Edit ', edit_project_path(project)
/       span
/         = link_to ' Destroy ', project, data: {:confirm => 'Are you sure?'}, :method => :delete
/       = form_for [project, @task], remote: true,  data: { project_id: project.id } do |f|
/         .field
/           = f.label :title
/           = f.text_field :title
/         .actions = f.submit
/       .tasks id="tasks_#{project.id}"
/         = render 'tasks/list', project: project

/ br

/ = link_to 'New Project', new_project_path